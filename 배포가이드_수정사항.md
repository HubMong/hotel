# 배포 가이드 수정 필요 사항

## 🔴 필수 수정 (배포 실패 방지)

### 1. application.yml 수정
```yaml
# 현재
spring:
  config:
    import: optional:file:.env[.properties]

# 수정 후
spring:
  config:
    import: optional:file:/opt/my-backend/.env[.properties]
```

### 2. OAuth redirect-uri 환경변수화
```yaml
# 현재
redirect-uri: https://hwiyeong.shop/login/oauth2/code/kakao

# 수정 후
redirect-uri: ${APP_BASE_URL:https://hwiyeong.shop}/login/oauth2/code/kakao
```

.env에 추가:
```env
APP_BASE_URL=https://hwiyeong.shop
```

### 3. ResourceConfig.java 수정 (업로드 경로 통일)
```java
// 현재
String uploadPath = System.getProperty("user.dir") + "/uploads/";

// 수정 후 (환경변수 사용)
@Value("${file.upload-dir:/opt/my-backend/uploads}")
private String uploadDir;

@Override
public void addResourceHandlers(ResourceHandlerRegistry registry) {
    Path uploadPath = Paths.get(uploadDir).toAbsolutePath();
    registry.addResourceHandler("/uploads/**")
            .addResourceLocations("file:" + uploadPath.toString() + "/");
}
```

## ⚠️ 가이드 개선 사항

### 4. 배포 가이드 6-2 섹션에 추가
```bash
# .env 파일 생성 후 실제 값으로 교체 필요
# 아래 명령으로 플레이스홀더 확인
grep "your-" /opt/my-backend/.env

# 각 서비스별 키 발급:
# - Google OAuth: https://console.cloud.google.com/apis/credentials
# - Naver OAuth: https://developers.naver.com/apps
# - Kakao OAuth: https://developers.kakao.com/console/app
# - JWT Secret: openssl rand -base64 64
# - Toss Payments: https://developers.tosspayments.com/
# - SendGrid: https://app.sendgrid.com/settings/api_keys
# - reCAPTCHA: https://www.google.com/recaptcha/admin
```

### 5. 배포 가이드 5-2 섹션 Nginx 설정 명확화
```bash
# 백엔드 Private IP 확인 (AWS CLI)
BACK_PRIVATE_IP=$(aws ec2 describe-instances \
  --instance-ids <BACK_INSTANCE_ID> \
  --query 'Reservations[0].Instances[0].PrivateIpAddress' \
  --output text)

echo "백엔드 Private IP: $BACK_PRIVATE_IP"

# Nginx 설정에 자동 반영
sudo sed -i "s/<BACK_PRIVATE_IP>/$BACK_PRIVATE_IP/g" /etc/nginx/sites-available/hwiyeong.shop

# 확인
grep "set \$backend" /etc/nginx/sites-available/hwiyeong.shop
```

### 6. 배포 가이드 6-1 섹션에 추가
```bash
# 디렉터리 생성 및 권한 설정
sudo mkdir -p /opt/my-backend/{releases,shared/logs,uploads}
sudo chown -R ubuntu:ubuntu /opt/my-backend
sudo chmod 755 /opt/my-backend/uploads
```

### 7. 배포 가이드 5-3 섹션 Certbot 명령 수정
```bash
# 실제 이메일 주소로 교체 필요
sudo certbot --nginx -d hwiyeong.shop \
  --agree-tos -m admin@hwiyeong.shop \
  --redirect --non-interactive
```

### 8. 배포 가이드 2장 RDS 생성 후 추가
```bash
# DB 스키마 초기화 (선택사항: DDL auto-update 사용 시 불필요)
# db.md 파일을 서버에 업로드 후
mariadb -h <RDS_ENDPOINT> -u <DB_USERNAME> -p hotel < db.md
```

## 📝 추가 권장 사항

### 9. 헬스체크 스크립트 추가
```bash
# /opt/my-backend/healthcheck.sh
#!/bin/bash
HEALTH=$(curl -sf http://localhost:8888/actuator/health | jq -r '.status')
if [ "$HEALTH" != "UP" ]; then
  echo "Backend is down! Status: $HEALTH"
  exit 1
fi
echo "Backend is healthy"
```

### 10. systemd 서비스에 재시작 제한 추가
```ini
[Service]
...
StartLimitInterval=300
StartLimitBurst=5
```

## 🧪 검증 체크리스트

배포 후 반드시 확인:

```bash
# 1. 환경변수 로드 확인
sudo journalctl -u my-backend -n 50 | grep -i "env\|property"

# 2. 업로드 디렉터리 확인
ls -la /opt/my-backend/uploads
curl -I https://hwiyeong.shop/uploads/

# 3. OAuth 리다이렉트 확인
curl -I https://hwiyeong.shop/api/oauth2/authorization/google

# 4. 백엔드 헬스체크
curl https://hwiyeong.shop/api/actuator/health

# 5. 로그 에러 확인
sudo journalctl -u my-backend --since "10 minutes ago" | grep -i error
```

## 결론

위 수정 사항을 반영하면 배포 성공률이 크게 향상됩니다. 특히:
- ✅ .env 절대경로 지정으로 환경변수 로드 보장
- ✅ OAuth redirect-uri 환경변수화로 도메인 변경 유연성 확보
- ✅ 업로드 경로 통일로 파일 업로드/서빙 안정성 향상
